package slp;

import slp.Slp.Exp;
import slp.Slp.Stm;
import util.Todo;

public class MaxArgument {
    // ///////////////////////////////////////////
    // expression
    private int maxExp(Exp.T exp) {
        throw new Todo(exp);
    }

    // ///////////////////////////////////////////
    // statement
    public int maxStm(Stm.T stm) {
        throw new Todo(stm);
    }
}
